{
  "private": true,
  "scripts": {
    "dev": "concurrently \"cd services/api && yarn dev\" \"cd services/web && yarn dev\"",
    "start": "docker-compose up"
  },
  "workspaces": [
    "services/api",
    "services/web"
  ],
  "devDependencies": {
    "concurrently": "^5.3.0"
  }
}
